// filter
// orden

// export async function orderCountries (criterio, targetOrder){

// let orderCountries 
// if(criterio.name === 'Ascendient'){
//     orderCountries = targetOrder.sort((a,b)=>(
//         a.name > b.name ? 1 : a.name < b.name ? -1 : 0
//     ))
// }
// if(criterio.name === 'Descendient'){
//     orderCountries = targetOrder.sort((a,b)=> (
//         a.name < b.name ? 1 : a.name > b.name ? -1 : 0
//     ))
// }
// if (criterio.region === 'Ascendent')
// orderCountries = targetOrder.sort((a, b) => (
//     a.region > b.region ? 1 : a.region < b.region ? -1 : 0),
// )

// if (criterio.region === 'Descendent')
// orderCountries = targetOrder.sort((a, b) => (
//     a.region < b.region ? 1 : a.region > b.region ? -1 : 0),
// )
// if (criterio.area === 'Ascendent')
// orderCountries = targetOrder.sort((a, b) => (
//     a.area > b.area ? 1 : a.area < b.area ? -1 : 0),
// )

// if (criterio.area === 'Descendent')
// orderCountries = targetOrder.sort((a, b) => (
//     a.area < b.area ? 1 : a.area > b.area ? -1 : 0),
// )
// return orderCountries; 

// }
